# Handoff

`PW2-WP10-PACK-REGISTRY-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP10-PACK-UI-BINDING-001`:

- `GET /api/v1/aof/packs/records`
- `GET /api/v1/aof/packs/records/:id`

The runtime path remains read-only and does not install packs or call an external package registry.
