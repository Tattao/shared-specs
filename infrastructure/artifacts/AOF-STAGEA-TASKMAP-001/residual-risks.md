# Residual Risks

- The map is intentionally not a full implementation backlog. It selects Stage A safe entry tasks and names Stage B candidates.
- WP0-WP10 still need concrete product-code tasks after architecture, QA, and legal gates are accepted.
- The map assumes `93` remains the 40-day Alpha task source. If `93` changes, regenerate this artifact.
- Product-code tasks should use dedicated osmx worktrees, not the shared-specs checkout.
