# PW1-WP13-STORE-ABSTRACTION-DOC-001 Residual Risks

- The output is an architecture contract and task breakdown, not executable interface code.
- Existing product code may still contain direct engine access until later store skeleton and portability smoke tasks move it behind contracts.
- Store adapter implementation will require separate product-code approval if it touches `osmx-go/**`.
