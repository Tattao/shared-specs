# Handoff

`PW2-WP9-RESILIENCE-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP9-RESILIENCE-UI-BINDING-001`:

- `GET /api/v1/aof/resilience/records`
- `GET /api/v1/aof/resilience/records/:id`

The runtime path remains replay-only and does not run experiments.
