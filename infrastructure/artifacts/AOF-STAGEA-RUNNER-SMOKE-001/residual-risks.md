# Residual Risks

- The smoke validates queue mutation only.
- It does not validate Codex session spawning, worktree creation, or command execution.
- Human-gated tasks remain intentionally blocked unless `--allow-human-gate` is used after approval.
