# Handoff

`PW2-WP2-TWIN-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP2-TWIN-UI-BINDING-001`:

- `GET /api/v1/aof/twin/nodes`
- `GET /api/v1/aof/twin/nodes/:id`

Both endpoints are read-only and inherit the existing protected AOF feature gate.
