# Handoff

`PW2-WP4-ALERTOPS-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP4-ALERTOPS-UI-BINDING-001`:

- `GET /api/v1/aof/alerts/records`
- `GET /api/v1/aof/alerts/records/:id`

The endpoints are read-only and inherit the existing protected AOF feature gate.
