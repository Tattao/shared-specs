# PW1-WP1-CORE-CONTRACT-DOC-001 Residual Risks

- The contract is a draft until product-code tasks and tests enforce it.
- API stubs, migrations, and UI shells still need to bind to the contract.
- New lifecycle states must update contract, audit projection, timeline projection, and tests together.
- Product Wave 1 remains forbidden from auto-merge, release, production operations, destructive commands, and scope expansion.
