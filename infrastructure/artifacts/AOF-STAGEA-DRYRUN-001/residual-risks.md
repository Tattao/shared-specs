# Residual Risks

- Stage A is complete, but Stage B should begin with a repair/preparation wave instead of direct unsupervised product-code execution.
- `runner-v2.py` needs timestamp parsing hardening before active-lease stale checks can be trusted in long unattended runs.
- v2 gates need an executable gate runner; several expectations still require manual interpretation.
- WP12 smoke coverage is currently an inventory and plan, not an automated evidence pipeline.
- `osmx` contains broad local planning-document changes from prior strategy work; syncing accepted Stage A outcome back to OSMX should be batched intentionally.
- NeatLogic code reuse remains blocked on source version pinning, private legal evidence pointers, security/dependency scans, third-party notice, and customer delivery statement.
- Claude Code and Hermes are healthy as tools, but should remain scoped worker/evaluator/summarizer roles until Stage B proves the loop.
