# PW1-WP10-PACK-MANIFEST-001 Residual Risks

- The manifest contract is not yet exposed as an HTTP endpoint.
- The contract validates manifest shape only; it does not load files, install packs, or resolve dependencies.
- Dependency constraints are structural only and not semantically evaluated.
