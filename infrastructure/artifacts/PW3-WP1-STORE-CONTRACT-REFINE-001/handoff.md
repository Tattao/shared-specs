# PW3-WP1 Handoff

Status: closed

Next ready task:

- `PW3-WP2-STORE-BACKED-RUNTIME-CORE-001`

Implementation notes for WP2:

- Add store adapter interfaces and in-memory/dev adapter boundaries without migrations.
- Preserve Wave 2 runtime endpoint compatibility.
- Keep sample fallback explicit and mark runtime provenance as `sample` or `hybrid` when used.
- Do not add raw SQL inside product handlers.
