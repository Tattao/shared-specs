# PW5-WP5 Handoff

After explicit `migration_runtime_replay` approval and disposable PostgreSQL/MySQL DSNs are provided, run the live replay package and attach logs, connection redaction proof, and pass/fail output to this artifact directory.
