# Handoff

`PW2-WP5-WORKCENTER-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP5-WORKCENTER-UI-BINDING-001`:

- `GET /api/v1/aof/workcenter/records`
- `GET /api/v1/aof/workcenter/records/:id`

The endpoints are read-only and inherit the existing protected AOF feature gate.
