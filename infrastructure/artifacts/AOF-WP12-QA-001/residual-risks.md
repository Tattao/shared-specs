# Residual Risks

- Smoke plans are not automated yet.
- Evidence folders are proposed but not populated.
- UI/API bindings are intentionally deferred until architecture and product implementation tasks run.
- A `pass` verdict here means the QA inventory was created, not that complete-product flows already pass.
