# Handoff

`PW2-WP8-SKILL-REGISTRY-RUNTIME-API-001` is ready for the dependent UI binding task.

Use these endpoints for `PW2-WP8-SKILL-REGISTRY-UI-BINDING-001`:

- `GET /api/v1/aof/skills/records`
- `GET /api/v1/aof/skills/records/:id`

The runtime path remains read-only and does not execute skills.
