# Residual Risks

- The boundary matrix is a document-level gate; WP13.2-WP13.10 still need executable API, package, schema, and validation checks.
- Hermes is intentionally not enabled as an active controller in Stage A. If adopted later, `docs/plans/96-*` should receive a scoped update.
- NeatLogic reuse is still blocked on the legal / reuse governance task before any code movement.
- The `osmx` working tree contains broader local strategy-document changes from the planning wave, so product-doc commits should be grouped intentionally.

